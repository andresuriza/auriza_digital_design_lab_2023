module contador_regresivo(input clk, reset,
									output reg[5:0] out);
								  
always @ (posedge clk) begin
	if (!reset)
		out <= 9;
	
	else begin
		if (out == 0) begin
		end
		
		else
			out <= out - 1;
	end
end
	
endmodule